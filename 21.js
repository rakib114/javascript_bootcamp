// for of loop in array

let players = ["Sakib", "Tamim", "Mashrafi", "Mushfiq"];
let cnfList = [];
for (const player of players) {
  //   console.log(player.toUpperCase());
  cnfList.push(player.toUpperCase());
}
console.log(cnfList);
